name: Snowflake Infrastructure Deploy

on:
  push:
    branches: [ main ]
  workflow_dispatch:

env:
  SNOWFLAKE_ACCOUNT: "TMJESFF-ED32433"
  SNOWFLAKE_USER: ${{ secrets.SNOWFLAKE_USER }}
  SNOWFLAKE_PASSWORD: ${{ secrets.SNOWFLAKE_PASSWORD }}
  SNOWFLAKE_ROLE: "ACCOUNTADMIN"
  SNOWFLAKE_WAREHOUSE: "COMPUTE_WH"

jobs:
  check_changes:
    runs-on: ubuntu-latest
    outputs:
      infra_sql: ${{ steps.filter.outputs.infra_sql }}
      infra_all: ${{ steps.filter.outputs.infra_all }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            infra_sql:
              - 'infrastructure/setup_infra.sql'
            infra_all:
              - 'infrastructure/**'

  deploy:
    runs-on: ubuntu-latest
    needs: check_changes
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # --- 第一阶段：Terraform ---
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform Init & Apply
        env:
          SNOWFLAKE_ORGANIZATION_NAME: "TMJESFF"
          SNOWFLAKE_ACCOUNT_NAME:      "ED32433"
        run: |
          cd terraform
          terraform init
          # 修正：使用转义符处理标识符
          terraform import snowflake_database.cosmetics_db COSMETICS_DB_DEV || true
          terraform import snowflake_schema.cosmetics_schema "\"COSMETICS_DB_DEV\".\"COSMETICS\"" || true
          terraform apply -auto-approve

      # --- 第二阶段：Python 环境准备 ---
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          pip install snowflake-snowpark-python
          # 🔴 严格执行你要求的 1.10.0 版本
          pip install great-expectations==1.10.0

      # --- 第三阶段：执行基础设施 SQL (仅当 setup_infra.sql 变动时) ---
      - name: Run Physical Infrastructure SQL
        if: needs.check_changes.outputs.infra_sql == 'true' || github.event_name == 'workflow_dispatch'
        env:
          SNOWFLAKE_ACCOUNT: ${{ env.SNOWFLAKE_ACCOUNT }}
          SNOWFLAKE_USER: ${{ env.SNOWFLAKE_USER }}
          SNOWFLAKE_PASSWORD: ${{ env.SNOWFLAKE_PASSWORD }}
        run: |
          python -c "
          import os, re
          import snowflake.snowpark as snowpark
          session = snowpark.Session.builder.configs({
              'account': '${{ env.SNOWFLAKE_ACCOUNT }}',
              'user': '${{ env.SNOWFLAKE_USER }}',
              'password': '${{ env.SNOWFLAKE_PASSWORD }}',
              'role': 'ACCOUNTADMIN'
          }).create()
          
          def is_valid_sql(sql):
              content = re.sub(r'--.*', '', sql)
              content = re.sub(r'/\*.*?\*/', '', content, flags=re.DOTALL)
              return len(content.strip()) > 0

          try:
              sql_file = 'infrastructure/setup_infra.sql'
              print(f'🚀 正在执行物理层部署: {sql_file}')
              with open(sql_file, 'r') as f:
                  queries = [q.strip() for q in f.read().split(';') if q.strip()]
                  for query in queries:
                      if is_valid_sql(query):
                          session.sql(query).collect()
              print('✅ 物理层部署成功！')
          except Exception as e:
              print(f'❌ 执行失败: {str(e)}')
              exit(1)
          finally:
              session.close()
          "

      # --- 第四阶段：运行 Table & Stream Setup ---
      - name: Run Table & Stream Setup
        if: needs.check_changes.outputs.infra_all == 'true' || github.event_name == 'workflow_dispatch'
        env:
          SNOWFLAKE_ACCOUNT: ${{ env.SNOWFLAKE_ACCOUNT }}
          SNOWFLAKE_USER: ${{ env.SNOWFLAKE_USER }}
          SNOWFLAKE_PASSWORD: ${{ env.SNOWFLAKE_PASSWORD }}
          DEPLOY_ENV: "DEV"
        run: |
          # 修正：直接运行路径下的脚本
          python infrastructure/setup_tables.py

      # --- 第五阶段：运行 GX Setup (生成本地文件) ---
      - name: Generate GX Configs Locally
        if: needs.check_changes.outputs.infra_all == 'true' || github.event_name == 'workflow_dispatch'
        env:
          SNOWFLAKE_ACCOUNT: ${{ env.SNOWFLAKE_ACCOUNT }}
          SNOWFLAKE_USER: ${{ env.SNOWFLAKE_USER }}
          SNOWFLAKE_PASSWORD: ${{ secrets.SNOWFLAKE_PASSWORD }}
          SNOWFLAKE_WAREHOUSE: ${{ env.SNOWFLAKE_WAREHOUSE }}
        run: python infrastructure/setup_gx.py

      # --- 第六阶段：配置 AWS 凭证 ---
      - name: Configure AWS Credentials
        if: needs.check_changes.outputs.infra_all == 'true' || github.event_name == 'workflow_dispatch'
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-southeast-1

      # --- 第七阶段：同步到 S3 ---
      - name: Sync GX Configs to S3
        if: needs.check_changes.outputs.infra_all == 'true' || github.event_name == 'workflow_dispatch'
        run: |
          aws s3 sync /tmp/gx_configs s3://bucket-for-snowflake-projects/cosmetics_etl_project/gx_configs/great_expectations/ --delete